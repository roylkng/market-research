name: H021 StockAnalysis parser probe

on:
  workflow_dispatch:
  push:
    branches:
      - research/h021-stockanalysis-parser-v1-20260914
    paths:
      - 'src/marketlab/h021_stockanalysis_parser.py'
      - 'scripts/probe_h021_stockanalysis_parser.py'
      - 'tests/test_h021_stockanalysis_parser.py'
      - 'research/prospective/h021/stockanalysis-parser-probe-v1.json'
      - '.github/workflows/h021-stockanalysis-parser-probe.yml'

permissions:
  contents: write

concurrency:
  group: h021-stockanalysis-parser-probe
  cancel-in-progress: false

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install
        run: python -m pip install -e '.[dev]'

      - name: Validate parser invariants
        run: pytest -q tests/test_h021_stockanalysis_parser.py

      - name: Run live target-aware parser probe
        id: parser_probe
        shell: bash
        run: |
          set +e
          OUT="reports/h021-stockanalysis-parser-probe/run-${GITHUB_RUN_ID}-attempt-${GITHUB_RUN_ATTEMPT}.json"
          python scripts/probe_h021_stockanalysis_parser.py \
            --config research/prospective/h021/stockanalysis-parser-probe-v1.json \
            --out "$OUT"
          CODE=$?
          echo "exit_code=$CODE" >> "$GITHUB_OUTPUT"
          echo "report_path=$OUT" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Anchor source-only parser evidence
        if: always()
        shell: bash
        run: |
          OUT="${{ steps.parser_probe.outputs.report_path }}"
          if [ -z "$OUT" ] || [ ! -f "$OUT" ]; then
            echo "No parser-probe report was produced"
            exit 1
          fi
          git config user.name "marketlab-research-bot"
          git config user.email "marketlab-research-bot@users.noreply.github.com"
          git add "$OUT"
          if ! git diff --cached --quiet; then
            git commit -m "H021: anchor StockAnalysis parser probe ${GITHUB_RUN_ID} [skip ci]"
            git push origin HEAD:research/h021-stockanalysis-parser-v1-20260914
          fi

      - name: Upload source-only parser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: h021-stockanalysis-parser-probe-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ steps.parser_probe.outputs.report_path }}
          if-no-files-found: error
          retention-days: 90

      - name: Enforce parser probe result
        if: always()
        shell: bash
        run: |
          test "${{ steps.parser_probe.outputs.exit_code }}" = "0"
