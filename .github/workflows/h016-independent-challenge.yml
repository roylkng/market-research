name: H016 independent dual-horizon momentum challenge

on:
  push:
    branches:
      - research/h005-result-repricing-ranker-v1-20260908
    paths:
      - .github/workflows/h016-independent-challenge.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  challenge:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/h005-result-repricing-ranker-v1-20260908
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install frozen research environment
        run: python -m pip install -e '.[dev]'
      - name: Normalize only H016 runner formatting before first outcome
        run: |
          ruff check --fix scripts/run_h016_nse_style_dual_momentum.py
          ruff format scripts/run_h016_nse_style_dual_momentum.py
          ruff check scripts/run_h016_nse_style_dual_momentum.py
          if ! git diff --quiet -- scripts/run_h016_nse_style_dual_momentum.py; then
            git config user.name "marketlab-research-bot"
            git config user.email "marketlab-research-bot@users.noreply.github.com"
            git add scripts/run_h016_nse_style_dual_momentum.py
            git commit -m "style: normalize frozen H016 runner"
            git push origin HEAD:research/h005-result-repricing-ranker-v1-20260908
          fi
      - name: Verify repository before first H016 outcome
        run: |
          ruff check scripts/run_h016_nse_style_dual_momentum.py
          ruff check src tests scripts
          pytest -q
          marketlab validate-registry registry/hypotheses.yaml
      - name: Run frozen H016 challenge
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/h016-independent
          mkdir -p /tmp/h016-independent
          python scripts/run_h016_nse_style_dual_momentum.py --out /tmp/h016-independent \
            | tee /tmp/h016-independent/run.log
          test -s /tmp/h016-independent/point-in-time-selections.json
          test -s /tmp/h016-independent/challenge-summary.json
          test -s /tmp/h016-independent/selected.csv
      - name: Upload H016 results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: h016-independent-challenge-results
          path: |
            /tmp/h016-independent/point-in-time-selections.json
            /tmp/h016-independent/challenge-summary.json
            /tmp/h016-independent/selected.csv
            /tmp/h016-independent/market-acquisition-diagnostics.json
            /tmp/h016-independent/market-source-manifest.json
            /tmp/h016-independent/source-manifest.json
            /tmp/h016-independent/run.log
          retention-days: 30
      - name: Upload retained official source bytes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: h016-independent-challenge-sources
          path: /tmp/h016-independent/raw
          retention-days: 30
          compression-level: 0
